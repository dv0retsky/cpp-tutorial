# Тема №12. Работа с файлами 📋

<div align="center">
  <img alt="Project Demo" src="./CPP12.jpg" />
</div>

На самом деле, файловый ввод-вывод ничем не отличается от консольного. За единственным исключением – если данные читаются из файла, то в любой момент можно вернуться к началу файла и считать все заново. Для того, чтобы в **C++** работать с файлами, необходимо подключить заголовочный файл `fstream`: 

```cpp
#include <fstream>
```
После этого можно объявлять объекты, привязанные к файлам: для чтения данных из файла используются объекты типа `ifstream` аббревиатура от input file stream, для записи данных в файл используются объекты типа `ofstream` (output file stream). Например 

```cpp
ifstream in; // Поток in будем использовать для чтения 
ofstream out; // Поток out будем использовать для записи 
```

Чтобы привязать тот или иной поток к файлу (открыть файл для чтения или для записи) используется метод open, которому необходимо передать параметр – текстовую строку, содержащую имя открываемого файла. 

```cpp
in.open("input.txt"); 
out.open("output.txt");
```

После открытия файлов и привязки их к файловым потокам, работать с файлами можно так же, как со стандартными потоками ввода-вывода `cin` и `cout`. Например, чтобы вывести значение переменной `x` в поток `out` используется следующая операция 

```cpp
out << x; 
```

А чтобы считать значение переменной из потока `in` 

```cpp
in >> x; 
```

Для закрытия ранее открытого файла используется метод `close()` без аргументов: 

```cpp
in.close(); 
out.close(); 
```

Закрытый файловый поток можно переоткрыть заново при помощи метода `open`, привязав его к тому же или другому файлу.

При считывании данных из файла может произойти ситуация достижения конца файла (end of file, сокращенно `EOF`). После достижения конца файла никакое чтение из файла невозможно. Для того, чтобы проверить состояние файла, необходимо вызвать метод `eof()`.

Данный метод возвращает `true`, если достигнут конец файла или `false`, если не достигнут. Кроме того, состояние файлового потока можно проверить, если просто использовать идентификатор потока в качестве логического условия: 

```cpp
if (in) { } 
```


Также можно использовать в качестве условия результат, возвращаемой операцией считывания. Если считывание было удачным, то результат считается истиной, а если неудачным – ложью. Например, организовать считывание последовательности целых чисел можно так: 

```cpp
int d; 
while(in >> d) { }
```

А организовать считывание файла построчно (считая, что строка заканчивается символом перехода на новую строку) так: 

```cpp
string S; 
while ( getline(in,s)) { 
}
```

Пример №1. Напишите программу, которая записывает в файл числа от 1 до 100, каждое число с новой строки.

```cpp
#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ofstream file("output.txt");
    if (!file. is_open()) {
        cerr << "Unable to open file!" << endl;
        return 1;
    }
    for (int i = 1; i <= 100; ++i) {
        file << i << endl;
    }
    
    file.close();
    cout << "Numbers written to file successfully!" << endl;

    return 0;
}
```

Пример №2. Напишите программу, которая считывает файл и проверяет, содержится ли в нем заданное слово.

```cpp
#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    ifstream file("input.txt");
    if (!file.is_open()) {
        cerr << "Unable to open file!" << endl;
        return 1;
    }
    
    string word_to_find = "example";
    string word;
    bool found = false;

    while (file >> word) {
        if (word == word_to_find) {
            found = true;
            break;
        }
    }
    
    if (found) {
        cout << "Word found in file!" << endl;
    } else {
        cout << "Word not found in file." << endl;
    }
    
    file.close();
    return 0;
}
```

## 📌 Задачи для практики

### 🧩 Задача 1.

Алексею надоело читать пользовательские соглашения, поэтому он решил собрать все текстовые файлы с ними и попортить каждое из них следующим образом: он берёт текстовый файл и удаляет из него буквы, которые содержаться во фразе `"user agreement"`, то есть буквы `"u"`, `"s"`, `"e"` и т.д. вне зависимости от их регистра.

Необходимо помочь автоматизировать Лёше этот процесс. На вход программа получает файл `input.txt`, в файл `output.txt` выводит текст из файла `input.txt` без соответствующих букв.

**Формат ввода**

```cpp
useragreement45kkl
```

**Формат вывода**

```cpp
45kkl
```

**Примечания:** Программа получает на вход данные из файла `input.txt` (не более 10000 символов на английском языке). Программа выводит в файл `output.txt` последовательность символов без соответствующих букв.

### 🧩 Задача 2.

Вы - частный детектив, которому доверили расследование наличия состава преступления в заимствованиях шуток Данилой Продольным у Джорджа Карлина. На вход вам даётся строка `X` из монолога Джорджа Карлина (длиной не более, чем 10000 символов) и строка из последнего стэндапа Данилы Продольного `Y` (также не более `10000` символов).

Необходимо выяснить, является ли Y подстрокой в `X`
На вход в файле `input1.txt` подается строка `X` и в файле `input2.txt` подается строка `Y` длины не более `10000`, состоящие только из маленьких букв латинского алфавита.

Необходимо вывести слово `yes`, если первая строка является подстрокой второй строки, или слово `no` в противном случае.

### 🧩 Задача 3.

Преподавателю по программированию стало нечем занять своих студентов, поэтому он придумал, что хочет получить обработчик текстовых файлов, который работает по следующему алгоритму: программа считывает из текстового файла `input.txt` строку, состоящую не более, чем из 255 символов.

Программа записывает в выходной файл `output.txt` указанную последовательность символов, кроме случаев, если в данной строке встречаются числа (последовательность цифр = 1 число). Тогда программа преобразует эти числа в двоичную систему счисления.

**Формат ввода**

```bash
6^&678JKjdkdl;?.,lk879Pk1kdfl4839
```

**Формат вывода**

```bash
110^&1010100110JKjdkdl;?.,lk1101101111Pk1kdfl1001011100111
```

**Примечания:** Единственная строка, состоящая из любых символов. Длина строки не превышает `255` символов. Гарантируется, что во всех числах нет ведущих нулей.
Выведите преобразованную строку.

---

<div align="center"> Made with ❤️ by <b>dv0retsky</b> </div>