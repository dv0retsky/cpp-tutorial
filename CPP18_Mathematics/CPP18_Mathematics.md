# Тема №18. Математические константы и операции

Язык **C++** предоставляет развитый набор математических инструментов:

- встроенные математические константы;
- сотни функций из стандартных заголовков;
- продвинутые средства обработки чисел с плавающей точкой;

В процедурном программировании знание этих возможностей позволяет писать точный, оптимальный и надёжный код для инженерных расчётов, графики, моделирования и научных вычислений.

<div align="center">
  <img alt="Project Demo" src="./CPP18.gif" />
</div>

## Модуль `<numbers>` и математические константы (C++20)

Начиная со стандарта `C++20`, в библиотеку включён модуль `std::numbers`, предоставляющий набор наиболее распространённых математических констант.

**Подключение:**

```cpp
#include <numbers>
```

### Наиболее распространённые константы модуля `<numbers>`

| Константа              | Значение       | Описание                       |
|------------------------|----------------|---------------------------------|
| `std::numbers::e`      | 2.7182818…     | Основание натурального логарифма |
| `std::numbers::pi`     | 3.1415926…     | Число π                         |
| `std::numbers::sqrt2`  | 1.4142135…     | Квадратный корень из 2         |
| `std::numbers::phi`    | 1.6180339…     | Золотое сечение                |

Все константы имеют тип `double`.

**Полезно:** `C++20` включает и другие значения (`ln2`, `inv_pi`, `egamma`, и др.), что исключает необходимость ручного определения.

## Математические функции `<cmath>`\

**Подключение:**

```cpp
#include <cmath>
```

### Основные функции

| Функция   | Описание                  |
|-----------|---------------------------|
| `abs(x)`  | Абсолютное значение       |
| `pow(a,b)`| Возведение в степень      |
| `sqrt(x)` | Квадратный корень         |
| `exp(x)`  | Экспонента (eˣ)           |
| `log(x)`  | Натуральный логарифм      |
| `log10(x)`| Десятичный логарифм       |

### Тригонометрические функции

> Аргументы — в радианах.

| Функция   | Описание  |
|-----------|-----------|
| `sin(x)`  | Синус     |
| `cos(x)`  | Косинус   |
| `tan(x)`  | Тангенс   |

### Округление

| Функция    | Описание                                          |
|------------|--------------------------------------------------|
| `ceil(x)`  | Округление вверх (к +∞)                          |
| `floor(x)` | Округление вниз (к –∞)                           |
| `round(x)` | Округление к ближайшему целому (`double`)        |
| `lround(x)`| Округление с возвратом `long`                    |
| `llround(x)`| Округление с возвратом `long long`              |

Особенности округления половинных значений:

`lround()` и `llround()` округляют половины к ближайшему значению от нуля.

## Проверка значений на `NaN` и `Infinity`

Некорректные арифметические операции могут приводить к появлению специальных значений:
- `+∞` и `–∞` — результат деления ненулевого числа на ноль.
- `NaN` — результат некорректных выражений, например `0.0 / 0.0`.

**Проверка:**

```cpp
std::isinf(x);  // true, если x → ±∞
std::isnan(x);  // true, если x — NaN
```

### Примеры математических операций

```cpp
#include <iostream>
#include <cmath>

int main()
{
    std::cout << "abs(-3) = " << std::abs(-3) << "\n";
    std::cout << "pow(-3, 2) = " << std::pow(-3, 2) << "\n";
    std::cout << "round(-3.2) = " << std::round(-3.2) << "\n";
    std::cout << "ceil(3.2) = " << std::ceil(3.2) << "\n";
    std::cout << "floor(3.2) = " << std::floor(3.2) << "\n";
    std::cout << "ceil(-3.2) = " << std::ceil(-3.2) << "\n";
    std::cout << "floor(-3.2) = " << std::floor(-3.2) << "\n";
}
```

**Вывод:**

```cpp
abs(-3) = 3
pow(-3, 2) = 9
round(-3.4) = -3
ceil(3.2) = 4
floor(3.2) = 3
ceil(-3.2) = -3
floor(-3.2) = -4
```

### Примеры проверки `Infinity` и `NaN`

```cpp
#include <iostream>
#include <cmath>

int main()
{
    double a{1.5}, b{-1.5}, c{}, d{};

    std::cout << a << "/" << b << " is Infinity? " << std::isinf(a / b) << std::endl;
    std::cout << a << "/" << b << " is NaN? " << std::isnan(a / b) << std::endl;

    std::cout << a << "/" << c << " is Infinity? " << std::isinf(a / c) << std::endl;
    std::cout << c << "/" << d << " is NaN? " << std::isnan(c / d) << std::endl;
}
```

**Вывод:**

```bash
1.5/-1.5 is Infinity? 0
1.5/-1.5 is NaN? 0
1.5/0 is Infinity? 1
0/0 is NaN? 1
```

---

## 📌 Задачи для закрепление изученного материала

### 🧩 Задача 1. Быстрое возведение в степень с модулем

**Ситуация:** Застройщики хотят быстро рассчитывать цены на квартиры при масштабировании проектов и применять остаток от деления для округления больших чисел.

**Задача:** Напишите функцию, которая вычисляет `x^n % m` за `O(log n)` операций.

**Дополнительно:** Проверьте работу функции для больших чисел `(10^12)` и большого модуля `(10^9+7)`.

---

### 🧩 Задача 2. Интеграция площади круга с погрешностью

**Ситуация:** Инженеры считают площадь круглого фонтана с нестандартной формой.

**Задача:** Используя `pi`, `pow` и `sqrt`, вычислите площадь с шагом дискретизации `dx` и суммируйте значения для интеграла, учитывая округление до 4 знаков после запятой.

---

### 🧩 Задача 3. Экспоненциальный рост с проверкой переполнения

**Ситуация:** Демографическая служба прогнозирует население мегаполиса.

**Задача:** Напишите функцию для `P = P0 * e^(r*t)`. Если результат превышает `1e12`, возвращайте `Infinity`. Используйте `std::isinf` и `std::exp`.

---

### 🧩 Задача 4. Проверка `NaN` и деление матриц

**Ситуация:** Финансовый аналитик делит доход на количество активов. Некоторые активы могут быть нулевыми.

**Задача:** Напишите функцию, которая делит элементы одной матрицы на элементы другой и проверяет результат на `NaN` и `Infinity`.

---

### 🧩 Задача 5. Квадратный корень и оптимизация гипотенузы

**Ситуация:** Архитектор проектирует лестницу с ограничением длины.

**Задача:** Пользователь вводит длину двух сторон. Выведите гипотенузу, округлённую до ближайшего целого. Если гипотенуза превышает лимит, сообщите о невозможности конструкции.

---

### 🧩 Задача 6. Абсолютные значения и среднеквадратичное отклонение

**Ситуация:** Инженер измеряет отклонения от нормы и хочет оценить их разброс.

**Задача:** Напишите функцию, которая вычисляет среднеквадратичное отклонение массива отклонений, используя `abs` и `sqrt`.

---

### 🧩 Задача 7. Тригонометрические вычисления для наклонной плоскости

**Ситуация:** Дизайнер проверяет наклон крыши и угол солнечного света.

**Задача:** Пользователь вводит угол наклона. Вычислите синус, косинус и тангенс, а также высоту тени от объекта длиной `L`. Если угол слишком большой и `tan(angle) > 100`, выведите предупреждение.

---


### 🧩 Задача 8. Многоуровневое округление цен

**Ситуация:** Магазин округляет цены с учетом налогов и скидок.

**Задача:** Для цены `x` сначала прибавьте налог `7.5%`, округлите вверх (`ceil`). Затем примените скидку `12.5%` и округлите до ближайшего целого (`round`). Выведите оба значения.

---

### 🧩 Задача 9. Логарифмическая оценка сложных процентов

**Ситуация:** Банкир хочет посчитать, сколько лет потребуется, если ставка изменяется каждый год.

**Задача** Используйте массив ставок `r[i]` и напишите функцию, которая вычисляет реальное время `t` для удвоения вклада, используя `log` и циклы.

---

### 🧩 Задача 10. Золотое сечение в проектировании с ограничениями

**Ситуация:** Дизайнер мебели хочет, чтобы пропорции стола соответствовали золотому сечению и вместили заданную площадь.

**Задача:** Пользователь вводит площадь `S`. Вычислите длину и ширину с помощью `phi`, округлите их, чтобы произведение не превышало `S`. Если не удаётся, выведите сообщение.

---

## Форматирование строк и функция `format`

Для более удобного форматирования строк начиная со стандарта `C++20` в стандартную библиотеку языка **C++** добавлен модуль `format` и в частности функция `std::format()`. 

В качестве первого аргумента функция принимает строку форматирования. Эта строка содержит любое количество плейсхолдеров `{}`. Второй и последующие параметры представляют аргументы, которые вставляются в эти плейсхолдеры - внутрь фигурных скобок - по одному аргументу для каждой пары фигурных скобок.

Рассмотрим небольшой пример:

```cpp
#include <iostream>
#include <format>
 
int main()
{
    int a {10};
    int b {7};
    std::cout << std::format("{} + {} = {}", a, b, a+b);
}
```

Здесь строка форматирования содержит три плейсхолдера `{}`: `"{} + {} = {}"`. В качестве второго, третьего и четвертого параметра - передаются значения, которые будут вставляться в плейсхолдеры в порядке следования: первое значения вставляет в первую пару фигурных скобок, второе значение - во вторую пару и так далее. 

В итоге мы получим следующий консольный вывод:

```bash
10 + 7 = 17
```

Стоит отметить, что поддержка этой функции в виду ее недавного добавления в стандарт в зависимости от компилятора может отличаться. Так, **Visual Studio** полностью поддерживает функцию, а в `GCC(g++)` поддержка была добавлена только начиная с версии `13.0`. А при компиляции с помощью Clang может потребоваться добавить флаг `-fexperimental-library`

```bash
clang++ -std=c++20 -fexperimental-library hello.cpp -o hello
```

Каждый плейсхолдер может содержать различные настройки в следующим виде:

```bash
[[fill]align][sign][#][0][width][.precision][type]
```

В квадратных скобках указаны отдельные параметры форматирования. Все эти параметры применяются к различным типам. Рассмотрим некоторые из них.

## Спецификаторы формата

Спецификатор формата позволяет установить количество отображаемых десятичных знаков числа с плавающей запятой и количество отображаемых символов строки. Он имеет следующий формат:

```bash
:.количество_знаков
```

После двоеточия и точки указывается количество знаков отображаемого числа:

```cpp
#include <iostream>
#include <format>
 
int main()
{
    double sum {100.2567}; 
    std::cout << std::format("sum = {:.5}", sum);
}
```

В данном случае форматирование применяется к числу `sum`. Оно равно `100.2567`. В строку форматирования передается спецификатор `:.5`, соответственно при выводе на консоль отображаться будут только первые `5` цифр числа:

```bash
sum = 100.26
```

Стоит отметить, что последняя отображаемая цифра увеличивается на `1`, если предыдущая цифра, которая отобрасывается при форматировании, больше или равна `5`. Поэтому в данном случае вместо `100.25` консоль выводит `100.26`

По умолчанию значение спецификатора формата указывает на общее количество значащих цифр (в нашем примере `5`), в том числе цифры как до, так и после точки. Но также можно в качестве спецификатора указать количество цифр после запятой. Для этого идобавляется букву ****f** к спецификатору формата:

```cpp
std::cout << std::format("sum = {:.5f}", sum);
```

Здесь опять же выводим пять знаков, но уже после запятой. Однако поскольку число `sum` в дробной части имеет только `4` знака, то в качестве пятого знака применяется `0`:

```bash
sum = 100.25670
```

Аналогично с помощью данного спецификатора можно задать количество отображаемых символов строки. Например, отобразим только пять символов строки:

```cpp
std::cout << std::format("name = {:.5}", "Tom Smith");    // name = Tom S
```

## Параметр `width`

Параметр `width` позволяет задать минимальную ширину поля. Чтобы достичь этой минимальной ширины в отформатированное значение при необходимости вставляются дополнительные символы - символы заполнения. Какие символы вставляются - зависит от типа значения и других параметров форматирования.

Для явной установки символа заполнения применяется параметр `fill`, который идет перед параметром `width`. Например, если параметру ширины предшествует символ `"0"` (ноль), то перед числом вставляются дополнительные нули. Если символ заполнения явным образом не указан, то вставляется так называемый символ заполнения по умолчанию.

```cpp
#include <iostream>
#include <format>
 
int main()
{
    int a {2}; 
    int b {5};
    int c {-8};
    std::cout << std::format("a = {:07}", a) << std::endl;
    std::cout << std::format("b = {:7}", b) << std::endl;
    std::cout << std::format("c = {:07}", c) << std::endl;
}
```

В данном случае выводятся три числа, для каждого из которых устанавливается минимальная длина в `7` символов. Однако для первого и третьего чисел в качестве символа заполнения применяется `"0"`, а для второго числа - символ заполнения по умолчанию (пробел). 

**Консольный вывод:**

```bash
a = 0000002
b =       5
c = -000008
```

Если для числа указан знак (`+` или `-`), то символы заполнения вставляются после знака.

## Выравнивание

Параметр `align` определяет, как выравнивается форматируемое значение: по левому краю (`<`), по правому краю (`>`) или по центру (`^`). Выравнивание по умолчанию зависит от типа значения.

```cpp
#include <iostream>
#include <format>
 
int main()
{
    std::cout << std::format("{:>7}|{:>7}|{:>7}|{:>7}|\n", 1, -.2, 3, 4);
    std::cout << std::format("{:<7}|{:<7}|{:<7}|{:<7}|\n", 1, -.2, 3, 4);
    std::cout << std::format("{:^7}|{:^7}|{:^7}|{:^7}|\n", 1, -.2, 3, 4);
}
```

Консольный вывод:

```bash
      1|   -0.2|      3|      4|
1      |-0.2   |3      |4      |
   1   | -0.2  |   3   |   4   |
```

## Настройка типа

Параметр `type` задает тип форматирования (не тип данных). Конкретный тип зависит от типа данных значения.

Для чисел с плавающей точкой применяется типы:

- **f:** форматирование с фиксированной точкой
- **g:** общее форматирование
- **e:** экспоненциальная запись
- **a:** шестнадцатеричная запись

Для целых чисел добавляются типы:

- **b:** вывод числа в двоичном формате
- **x:** вывод числа в шестнадцатеричном формате

**Применение типов:**

```cpp
#include <iostream>
#include <format>
 
int main()
{
    std::cout << std::format("{:f} \n", 34.56);     // 34.560000
    std::cout << std::format("{:g} \n", 34.56);     // 34.56
    std::cout << std::format("{:e} \n", 34.56);     // 3.456000e+01
    std::cout << std::format("{:a} \n", 34.56);     // 1.147ae147ae148p+5
    std::cout << std::format("{:b} \n", 122);       // 1111010
    std::cout << std::format("{:032b} \n", 122);    // 00000000000000000000000001111010
    std::cout << std::format("{:x} \n", 122);       // 7A
}
```

## Переопределение порядка аргументов

По умолчанию аргументы передаются в строку форматирования в порядке следования. Но мы можем переопределить вывод, указав в фигурных скобках до двоеточия номер аргумента (нумерация начинается с нуля):

```cpp
#include <iostream>
#include <format>
 
int main()
{
    int a{10};
    int b{20};
    int c{30};
    std::cout << std::format("{2:} {1:} {0:}", a, b, c);    // 30 20 10
}
```

В данном случае хотя в функции `std:format` первым аргументом для вывода является число `a`, однако в строке форматирования первым выводится третий аргумент - `{2:}`. И подобным образом можно указать вывод для других аргументов.

Явное указание индексов аргументов может быть полезным, когда нам надо вывести один и тот же аргумент несколько раз. Например, выведем одно и то же число в десятичной, двоичной и шестнадцатеричной форме:

```cpp
#include <iostream>
#include <format>
 
int main()
{
    int a{10};
    int b{20};
    int c{30};
    std::cout << std::format("{0:} {0:b} {0:X}", 62);    // 62 111110 3E
}
```

---

## 📌 Задачи для закрепление изученного материала

### 🧩 Задача 1. Отчёт о продажах

**Ситуация:** Вы — бухгалтер магазина, который формирует отчёт о продажах.

**Задача:** Для трёх товаров с ценами `a`, `b`, `c` и количеством проданных единиц `x`, `y`, `z` выведите строку:

```bash
Товар1: сумма = <сумма1>, Товар2: сумма = <сумма2>, Товар3: сумма = <сумма3>
```

Используйте `std::format` и два знака после запятой для всех сумм.

---

### 🧩 Задача 2. Таблица результатов студентов

**Ситуация:** Вы — преподаватель, который оформляет таблицу оценок студентов.

**Задача:** Для трёх студентов с именами `name1`, `name2`, `name3` и оценками `score1`, `score2`, `score3` выведите выровненную по правому краю таблицу шириной `10` символов для имени и `5` символов для оценки. 

Используйте выравнивание `>` и форматирование целых чисел.

---

### 🧩 Задача 3. Многоформатный вывод числа

**Ситуация:** Вы — разработчик системного ПО и хотите отобразить одно число в нескольких системах счисления.

**Задача:** Для числа `num` выведите его в десятичной, двоичной (с `16` битами с ведущими нулями) и шестнадцатеричной формах в одной строке с разделителем `|`. 

Используйте переопределение индексов аргументов `{0:b}`, `{0:X}` и ширину поля.

---

### 🧩 Задача 4. Финансовый отчёт с повторным выводом

**Ситуация:** Банк формирует отчёт, где одно и то же значение процента используется в нескольких расчётах.

**Задача:** Для процентной ставки rate выведите строку:

```bash
Процент: <rate>%, Двоичное представление: <rate:b>, В экспоненциальной форме: <rate:e>
```

Используйте один аргумент несколько раз через нумерацию `{0}`, `{0:b}`, `{0:e}`.

---

### 🧩 Задача 5. Календарь с выравниванием

**Ситуация:** Вы — разработчик утилиты, которая выводит календарь в консоль.

**Задача:** Для четырёх дней с числами `d1`, `d2`, `d3`, `d4` выведите строку с центровкой каждого числа в поле шириной `5` символов и символом заполнения `*`. 

Используйте выравнивание `^` и параметр `fill`.

---

<div align="center"> Made with ❤️ by <b>dv0retsky</b> </div>

