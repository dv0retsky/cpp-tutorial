# Тема №16. Задачки на закрепление тем 🤓

<div align="center">
  <img alt="Project Demo" src="./CPP16.gif" />
</div>

### 🧩 Задача 1. Разбиение числа

Напишите программу, которая находит количество способов разложить число `n` на сумму натуральных чисел.

**Пример:**

```bash
Вход: n = 4
Выход: 5
```

**Объяснение:**

```bash
Объяснение:
4 = 4
4 = 3 + 1
4 = 2 + 2
4 = 2 + 1 + 1
4 = 1 + 1 + 1 + 1
```

---


### 🧩 Задача 2: Самая длинная возрастающая подпоследовательность

Реализуйте функцию, которая находит длину самой длинной возрастающей подпоследовательности в массиве целых чисел.

**Пример:**

```bash
Вход: [10, 9, 2, 5, 3, 7, 101, 18]
Выход: 4
```

**Объяснение:** 

```bash
Самая длинная возрастающая подпоследовательность [2, 3, 7, 101] имеет длину 4
```

---


### 🧩 Задача 3: Размен монет

Реализуйте алгоритм для нахождения минимального количества монет для сдачи, используя заданные номиналы.

**Пример:**

```bash
Вход: coins = [1, 2, 5], amount = 11
Выход: 3
```

**Объяснение:** 

```bash
11 = 5 + 5 + 1
```

---


### 🧩 Задача 4: Уникальные пути

Напишите функцию, которая вычисляет число уникальных путей в прямоугольной сетке размером `m × n` из левого верхнего угла в правый нижний, если можно двигаться только вправо или вниз.

**Пример:**

```bash
Вход: m = 3, n = 7
Выход: 28
```

---

### 🧩 Задача 5: Максимальная сумма подмассива

Реализуйте программу, которая находит максимальную сумму подмассива в массиве целых чисел.

**Пример:**

```bash
Вход: [-2, 1, -3, 4, -1, 2, 1, -5, 4]
Выход: 6
```

**Объяснение:**

```bash
Объяснение: Подмассив [4, -1, 2, 1] имеет максимальную сумму 6
```

---

### 🧩 Задача 6: Лабиринт и P.Diddy

P.Diddy заблудился в лабиринте и хочет выбраться на улицу. Однако он, как известно, большой фанат pdf-файлов и не может пройти мимо ни одного такого! В лабиринте есть отметки о таких файлах, и P.Diddy должен собрать их как можно больше.

**Входные данные:**
Размеры лабиринта `(N x M)`.
Лабиринт, где `0` — пустая клетка, `1` — стена, `P` — pdf-файл, `E` — выход.

**Выходные данные:**
Максимальное количество pdf-файлов, которое сможет собрать P.Diddy на пути к выходу.

---


### 🧩 Задача 7: Крутая БРС

Руководству пришла в голову идея сделать балльно-рейтинговую систему еще круче, поэтому каждый кабинет в университете был разделен на `N` строк и `M` столбцов, где вход в аудиторию располагается в верхнем левом (если смотреть на карту) углу, а преподаватель сидит в правом нижнем.

По заданной карте и количеству баллов в клетках (если в клетке положительное число, то это количество баллов прибавляется к сумме баллов студента, если отрицательное - вычитается) необходимо рассчитать маршрут, обеспечивающий максимальную сумму накопленных баллов при условии, что можно двигаться только вниз и вправо (только в соседние клетки, не по диагонали).

**Например, если заданы значения:**

```bash
2 -3 2 3
10 2 2 2
-3 20 2 2
```

**Ответ должен быть в координатах ячеек:**

```bash
0 0 (старт, обязательная точка)
1 0
1 1
2 1
2 2
2 3 (правый нижний угол, обязательная точка финала).
```

---

### 🧩 Задача 8: Арсен Маркарян

Арсен Маркарян — большой любитель свежих пирожков. У него есть `N` пирожков, каждый из которых имеет свою калорийность. Арсен решил, что ему нужно оптимально распределить свои пирожки по дням, чтобы не переедать, но при этом наслаждаться каждым днем.
Арсен хочет распределить пирожки по `D` дням так, чтобы:

- В каждый день он ел хотя бы один пирожок.
- Разница в калориях между самым калорийным и самым низкокалорийным пирожком в каждый день была минимальной.

Помогите Арсену определить минимальную возможную разницу в калориях между самым калорийным и самым низкокалорийным пирожком в любой из дней.

**Входные данные:**

```bash
Первое число N (1 ≤ N ≤ 1000) — количество пирожков.
Второе число D (1 ≤ D ≤ N) — количество дней.
Массив из N целых чисел, представляющих калорийность каждого пирожка.
```

**Выходные данные:**

```bash
Минимальная возможная разница в калориях для распределения пирожков по дням.
```

---

### 🧩 Задача 9: Садовод

У "садовода" Павлика есть `N` различных растений, каждый из которых имеет свою стоимость (например, сколько радости он приносит, когда его подарят). 

Павлик хочет собрать букет из `K` растений, чтобы максимизировать общую стоимость букета. Однако он не может выбрать более `L` одинаковых растений.

**Входные данные:**

- Первое число `N (1 ≤ N ≤ 100)` — количество доступных растений.
- Второе число `K (1 ≤ K ≤ 20)` — количество растений в букете.
- Третье число `L (1 ≤ L ≤ K)` — максимальное количество одинаковых растений в букете.
- Массив из `N` целых чисел, представляющих стоимость каждого растения.

**Выходные данные:**
Максимальная возможная стоимость букета.

---

### 🧩 Задача 10: Тарелочница

Джессика очень любит покушать, в связи с чем у нее скопилось огромное кол-во посуды. У нее есть `plates` гор тарелок. Джессика ждет гостей через `h` часов и она не хочет, чтобы они увидели этот беспорядок.

Джессика может сама выбирать сколько тарелок ей помыть за час — `k`.
Каждый час она выбирает какую-нибудь горку тарелок и моет `k` тарелок из этой горки.
Если в горке меньше `k` тарелок, она домывает их все и идет отдыхать до конца часа.

Джессика хочет вымыть всю посуду, но не хочет переусердствовать. Найдите наименьшее целое число `k` такое,
что она сможет помыть все тарелки за `h` часов.

**Пример:**

```bash
Вход: plates = [30, 11, 23, 4, 20], h = 5
Выход: 30
```

**Объяснение:**

```bash
У Джессики 5 часов.
- 30 тарелок за первый час.
- 11 тарелок за второй час.
- 23 тарелки за третий час.
- 4 тарелки за четвертый час.
- 20 тарелок за пятый час.

Она моет 30 тарелок в час. Если в горке < 30 тарелок, она домывает эту горку и отдыхает остаток часа.
```

**Примечание:**

`len(plates)`, `plates[i]` могут быть очень большими.

---

<div align="center"> Made with ❤️ by <b>dv0retsky</b> </div>
